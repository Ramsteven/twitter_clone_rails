<div class="container text-center">
  <div class="d-flex flex-wrap">
    <% @users.each do |user| %>
      <%if current_user != user %>
        <%= render 'card', c: user %>
      <% end %>
    <% end %>
  </div>
</div>
