<%= form_with(model: current_user.followee_followships.build,
local: true) do |f| %>
  <div><%= hidden_field_tag :followee_id, c.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
